# 流水线模块实施任务

**前置条件**：规格、计划、数据模型和 REST 契约已完成。  
**状态**：核心实现已完成，待 MySQL 迁移验证与具体插件的独立规格。

## 阶段 1：核心定义与持久化

- [x] PL-T001 新建 `pipeline` Maven 模块，接入聚合工程和应用；依赖 `iam`、`project-management`，不得依赖具体执行 SDK。
- [x] PL-T002 编写 MySQL `V008` 与 H2 等价 Schema，创建流水线、版本、步骤、运行、步骤运行和日志表。
- [x] PL-T003 定义流水线、运行、步骤、插件描述符、校验错误和业务错误枚举/领域对象。
- [x] PL-T004 实现 YAML v1 解析与纯语法/结构校验；错误必须定位路径与行列。
- [x] PL-T005 实现插件目录抽象与插件描述符校验，核心不得按具体插件名称分支。

## 阶段 2：定义与版本 API

- [x] PL-T006 实现流水线创建、校验、列表、详情、更新、启停和版本查询的 DAO、服务及 REST 接口。
- [x] PL-T007 实现 IAM 与项目预配置适配：项目、仓库、环境授权及可用性校验。
- [x] PL-T008 编写定义、校验、版本化、乐观锁、跨项目隔离和权限拒绝测试。

## 阶段 3：运行编排与通用插件契约

- [x] PL-T009 实现运行创建、幂等键、配置快照、步骤运行初始化和查询 API。
- [x] PL-T010 实现顺序调度、状态机、失败传播、取消、人工整条重试和日志记录。
- [x] PL-T011 定义 `PipelinePlugin` 执行与取消契约；通过测试插件验证核心不依赖具体任务类型。
- [x] PL-T012 编写运行状态、幂等、取消、重试、权限复查和插件解耦测试。

## 阶段 4：验证与后续插件预留

- [ ] PL-T013 完成完整 Maven 测试、MySQL 迁移验证及 REST 集成测试，更新追踪记录。
- [x] PL-T014 已在 `../pipeline-plugins/` 规格化 Maven/Docker、产物存储和 Kubernetes 插件；不得在本任务中绕过插件边界直接实现。
