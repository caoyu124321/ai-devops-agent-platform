# 流水线模块需求检查清单

**对应规格**：[spec.md](spec.md)  
**检查状态**：通过；`PL-01` 至 `PL-09` 已采用本轮 MVP 默认方案。

## 已明确

- [x] 项目是流水线、运行、日志与产物的隔离边界。
- [x] 流水线定义、运行编排和插件具体执行相互解耦。
- [x] 流水线仅能使用 IAM 已授权且项目预配置模块允许的资源。
- [x] 流水线、插件和预配置资源在运行创建时固定版本快照。
- [x] MVP 仅面向公开 GitHub HTTPS、Maven/Java 构建测试和 Kubernetes 部署。
- [x] 禁止凭据明文、任意命令、任意 Docker 特权和任意宿主挂载。

## 待确认

- [x] 代码源与默认分支规则（PL-01、PL-03）。
- [x] 环境参数化规则（PL-02）。
- [x] 并行与重试语义（PL-04、PL-05）。
- [x] Kubernetes 部署描述、取消和回滚语义（PL-06、PL-08）。
- [x] 产物存储与项目删除时的运行处理（PL-07、PL-09）。

## 结论

本轮 MVP 决策已记录，可创建 `plan.md`、数据模型、接口契约和 `tasks.md`，随后开始编码。
