# IAM 可追溯检查清单

**目的**：确保每个用户故事和验收场景均有对应的数据约束、实现任务与自动化测试。  
**状态**：规格与数据模型评审通过；代码实现尚未开始。

## 1. 用户故事到任务映射

| 用户故事 | 验收场景 | 主要任务 | 数据模型 | 建议测试层级 |
| --- | --- | --- | --- | --- |
| US-01 注册 | AS-01.1 至 AS-01.4 | IAM-T004、IAM-T009、IAM-T020 | `iam_users` | 单元、接口、脱敏回归 |
| US-02 登录/登出 | AS-02.1 至 AS-02.5 | IAM-T006、IAM-T007、IAM-T009、IAM-T020 | `iam_sessions`、`iam_login_locks` | 单元、接口、时间边界 |
| US-03 改密 | AS-03.1 至 AS-03.3 | IAM-T005、IAM-T008、IAM-T009、IAM-T020 | `iam_users`、`iam_sessions` | 单元、接口、多会话集成 |
| US-04 租户 | AS-04.1 至 AS-04.4 | IAM-T010、IAM-T011、IAM-T020 | `iam_tenants`、`iam_tenant_members` | 服务、事务、接口 |
| US-05 邀请 | AS-05.1 至 AS-05.5 | IAM-T012、IAM-T020 | `iam_invitations`、`iam_tenant_members` | 服务、并发、过期清理 |
| US-06 成员管理 | AS-06.1 至 AS-06.5 | IAM-T013、IAM-T018、IAM-T020 | `iam_tenant_members` | 服务、事务、事件契约 |
| US-07 授权 | AS-07.1 至 AS-07.5 | IAM-T014 至 IAM-T019、IAM-T020 | `iam_authorization_grants` | 单元、授权契约、接口 |
| US-08 PROD 授权 | AS-08.1 至 AS-08.4 | IAM-T015、IAM-T016、IAM-T019、IAM-T020 | `iam_authorization_grants` | 单元、跨模块契约 |

## 2. 数据模型检查

- [x] 用户名和邮箱唯一性对应 US-01。
- [x] 多会话、Token 过期和撤销对应 US-02、US-03。
- [x] 登录失败计数与锁定时间对应 US-02。
- [x] 租户名称非唯一、成员关系唯一对应 US-04、US-06。
- [x] 邀请有效期、状态和唯一待处理邀请对应 US-05。
- [x] 项目/环境采用抽象资源标识，不提前引入后续业务表，对应 US-07、US-08。
- [x] 授权变更事件仅支持后续模块订阅，不由 IAM 停止任务，对应 US-06。

## 3. 代码实现前检查

- [x] 账号禁用、找回密码、自定义角色、二次确认均已明确为非范围。
- [x] PROD 不需二次确认，使用 `environment.deploy` 权限直接判断。
- [x] 多设备登录、24 小时 Token、改密撤销全部会话已有明确规则。
- [x] 最后管理员保护、邀请 7 天过期、重复邀请拒绝已有明确规则。
- [x] 跨租户与资源探测错误的信息隐藏规则已定义。
- [x] 数据模型、技术设计、任务清单与用户故事已建立映射。
- [x] 已补充现有 MySQL 表接入、DAO 边界、UTC 连接约定和授权项管理接口任务。
- [x] `IAM-T000` 数据模型评审完成（2026-07-18）。

## 4. 实现与验证门禁

1. 开始代码前：完成 `IAM-T000`，确认本清单无缺口。
2. 每完成一个任务：补充其对应 AS 编号的单元测试或接口测试，并在 `tasks.md` 勾选。
3. 完成 IAM-T020 前：所有 AS-01.1 至 AS-08.4 必须至少有一个自动化测试覆盖。
4. 验收时：验证跨租户隔离、改密全会话撤销、最后管理员保护、邀请过期、PROD 授权五个高风险场景。
