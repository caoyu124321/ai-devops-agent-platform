# 项目管理与预配置模块实施任务

**前置条件**：[spec.md](spec.md)、[plan.md](plan.md)、[data-model.md](data-model.md)、[REST 接口契约](contracts/rest-api.md) 已确认。  
**当前状态**：待实现；以下任务不表示已开始编码。

## 阶段 1：模块与持久化基础

- [x] T001 新建 `project-management` Maven 模块，接入聚合工程和 `application`；仅由 `devops.AiDevopsAgentPlatformApplication` 启动。
  - 完成标准：模块可编译；IAM 依赖方向为 `project-management → iam`，无反向依赖。
- [x] T002 定义项目、仓库、凭据、环境、目标类型、连接状态和错误码枚举，以及统一业务异常。
  - 完成标准：没有散落魔法值；错误码覆盖接口契约。
- [x] T003 编写 MySQL `V007` 与 H2 等价 Schema，创建 `data-model.md` 所列 13 张表、外键、唯一索引和检查约束。
  - 完成标准：空库迁移成功；非法租户/项目关联、重复名称和重复版本被数据库或服务层拒绝。
- [x] T004 编写 MyBatis Mapper、持久化对象和版本化 Repository/Environment/Credential DAO。
  - 完成标准：CRUD、版本追加、项目 20 仓库限制与硬删除级联均有集成测试。

## 阶段 2：安全与授权基础

- [x] T005 实现凭据加密边界：从受保护配置读取主密钥标识与密钥，写入 `pm_credential_versions.encrypted_payload`，禁止明文持久化和日志输出。
  - 完成标准：五种凭据类型均可加密保存和受控解密；序列化、异常与日志测试不包含秘密。
- [x] T006 实现 IAM 适配器：会话/OAuth 当前用户解析、统一授权请求、创建项目时绑定 `PROJECT_ADMIN`、删除项目时撤销项目角色。
  - 完成标准：租户管理员、项目管理员、开发者和跨租户拒绝路径均有单元测试；项目模块不直接判断角色字符串。
- [x] T007 实现凭据生命周期与项目授权服务。
  - 完成标准：轮换、停用、项目授权/撤销、租户隔离和项目管理员不可读取秘密均满足 AS-03.*。

## 阶段 3：项目与仓库

- [x] T008 实现项目创建、查询、更新和硬删除服务。
  - 完成标准：满足 AS-01.*；当前无运行模块时删除不依赖不存在的运行表；为未来取消契约保留文档说明而不提前实现编排。
- [x] T009 实现公开 GitHub HTTPS 地址规范化和 `GitHubRepositoryChecker`。
  - 完成标准：仅允许公开 HTTPS GitHub；读取默认分支；区分永久失效自动删除和临时失败保留；满足 AS-02.*。
- [x] T010 实现仓库服务和版本记录。
  - 完成标准：单项目最多 20 仓库、无默认仓库可保存、乐观锁和显式重新校验均有测试。

## 阶段 4：环境与连通性

- [x] T011 实现环境通用服务、版本记录、启停和连接健康状态。
  - 完成标准：三种目标类型、四种环境等级、项目内唯一名称、启用与健康分离均满足 FR-PM-009 至 FR-PM-014。
- [x] T012 实现 Kubernetes 环境配置与只读校验适配器。
  - 完成标准：默认/允许命名空间规则、凭据授权校验、无写集群操作和失败 `UNAVAILABLE` 均有契约测试。
- [x] T013 实现 SSH 与 HTTPS WinRM 环境校验适配器。
  - 完成标准：严格校验 SSH 主机指纹和 WinRM 证书指纹；不得提供跳过验证选项；当前不实现主机部署。

## 阶段 5：REST 与验证

- [x] T014 按 REST 契约实现项目、仓库、凭据和环境 Controller，Controller 不承载业务逻辑。
  - 完成标准：接口、状态码、乐观锁、游标分页和双 Token 认证与契约一致。
- [x] T015 编写 Web 层与服务层自动化测试，逐项覆盖 AS-01.* 至 AS-06.*。
  - 完成标准：成功、边界、权限拒绝、跨租户不可见、外部校验失败和秘密脱敏路径全部覆盖。
- [x] T016 执行全量 Maven 测试与迁移验证，更新规格追溯记录和未实现能力说明。
  - 完成标准：所有测试通过；Linux/Windows 主机仅配置校验、Kubernetes 执行仍由后续模块实现的边界明确可见。

## 推荐验收顺序

1. T001–T006：模块可启动、数据库可迁移、授权与秘密边界成立。
2. T007–T010：完成“创建项目 → 授权凭据 → 配置公开仓库”的可用切片。
3. T011–T013：完成三类环境配置与安全连通性校验。
4. T014–T016：通过 REST 完整验收，之后才进入流水线模块规格与实现。
